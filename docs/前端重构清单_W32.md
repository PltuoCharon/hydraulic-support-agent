# 前端重构清单_W32（W32-D1 盘点定稿，2026-09-15）

## 盘点基线
- 前端 13 视图 + 3 组件 + api/store/utils/composables，业务代码约 1554 行
- 出口线：pytest 69 绿；数据已冻结 data-v5，本周不碰数据与匹配逻辑
- 铁律：只动页面结构；接口契约不变；样式微调随行、不单独立项

## R1 删除脚手架死代码
- 问题：HelloWorld.vue（95 行）全项目零引用；hero.png 仅被 HelloWorld 引用；vite.svg/vue.svg 无引用
- 涉及：web/src/components/HelloWorld.vue、web/src/assets/{hero.png,vite.svg,vue.svg}、style.css 中 .hero 残留段
- 动：删除上述文件与残留样式
- 不动：其余组件与 assets
- 兜底：npm run build 通过；页面巡检无 404 资源

## R2 页签/路由双入口收口
- 问题：/input、/wizard、/chat 已并入 SelectCenter 页签，独立路由仍注册；App.vue 顶栏"智能体对话"与选型中心"对话选型"页签重复
- 动：router 中 /input、/wizard、/chat 改为 redirect 至 /select?tab=form|wizard|chat（保留深链兼容）；顶栏删除"智能体对话"菜单项
- 不动：SelectCenterView 页签结构与 query.tab 逻辑（第 35 行已支持 tab 参数）
- 前置检查：grep 全项目 router.push('/chat'|'/input'|'/wizard') 与 $router.push，确认无内部跳转依赖旧路径
- 兜底：手工巡检——三个页签切换正常、旧深链 /chat 跳转后落在对话页签

## R3 SelectCenterView 残留注释清理
- 问题：第 49 行孤立注释（W31-D5 sed 删重复 CSS 后的半截残留）
- 动：删第 49 行
- 兜底：视觉无变化

## R4 SpectrumView 硬编码"157"改动态
- 问题：第 6 行"本库 157 个 verified 型号"为硬编码，当前实际 166 verified（168-2 suspect），陈旧且违反"数据走接口"
- 动：改为从谱系接口返回数据前端计数 verified 渲染；est_fields 提示语保留
- 不动：散点图逻辑与 tooltip 结构
- 兜底：目视核对页面数字与 DB（SELECT COUNT(*) WHERE data_status='verified'）一致

## R5 CompareView 补可信度角标
- 问题：对比页无 badge，"估算三处一致"的前端环在对比页断裂
- 动：雷达图下方新增所选架型清单行，每项复用 utils/badge.js 渲染 el-tag（与 ResultView 同判定函数，零新逻辑）
- 前置确认：store.compare 项携带 support_source/support_status（源自 ResultView 勾选，预期携带，动手前 console 验证一次）
- 不动：雷达图维度、parseNum、归一化逻辑
- 兜底：同架型在结果页与对比页角标逐一比对一致

## 明确不做
- 不改任何接口、store 字段、匹配/归一化逻辑；不引入新依赖；不改 App.vue 全局主题

## 执行顺序
R3 → R1 → R2 → R4 → R5（先小后大；每步 npm run build + 巡检，全部完成后 pytest 69 绿收口）
