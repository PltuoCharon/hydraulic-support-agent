# 周报 W30（第30周：可视化与入口整合）

## 本周目标
按 W25–W40 总体规划，W30 为前端可视化与交互整合周：架型谱系图、厂商分布图、选型向导四步状态机、三入口 Tab 整合、首页真实统计，D6 收尾 polish，D7 周报与阶段收官。

## 完成项（逐日）
- **D1 架型谱系图**：X=工作阻力、Y=采高区间中值散点图，verified 白名单口径 157 点；est_fields 从 source 串派生（控顶长度估算 146 条、支护强度二阶估算 81 条），但本图两轴均不涉及估算字段，故全实心点——口径结论如实记录。/spectrum 路由注册在 /{model_id} 之前（FastAPI 顺序坑）。
- **D2 厂商分布图**：157 条 verified 中厂商字段 NULL 106 条（67.5%），覆盖率仅 32.5%，郑煤机 38 条居首；"未知"灰色虚线条居首如实呈现，"国产"占位值标橙，识别出"北京开采设计分院"系设计院混入。新增独立校验脚本 scripts/check_vendors.py。
- **D3 选型向导**：四步状态机（S0 输入→S1 确认→S2 结果→S3 完成），纯前端 stepper；只收引擎实际使用的 coal_thickness + dip_angle + top_n（诚实设计，不做收了不用的字段）。修复前后端口径漂移（后端 gt=0.5 严格大于，前端 min 对齐 0.6/24.9）与 422 detail 数组渲染。
- **D4 三入口整合**：SelectCenterView el-tabs（表单输入/选型向导/对话选型），route.query.tab 支持指定页签；地图→选型中心预填链路实测通过（东滩矿 6.5m/5.5°）。旧路由全部保留兼容。
- **D5 首页真实统计**：stats 概览栏五格（159 架型/23 矿区/41 案例/支护强度覆盖 95.6%/重量覆盖 8.2%），点击可跳对应页；/api/stats/ 尾斜杠必须。
- **D6 收尾 polish**：MapView 在用支架同型号去重合并（×n 计数）并补 computed import；utils/badge.js 统一角标判定（疑/估/缺/实，带 tip），ResultView 迁入共用（语义逐字保留）；选型中心页签内隐藏子视图 H2 双标题。commit c0f8f85 已推送，验收 PASS=53 FAIL=0。

## 问题与教训
1. **终端粘贴不可靠的再次验证**：WizardView 经 heredoc 串行且被误提交，base64 通道两次失败（段首被吃、段尾粘连致 mod4=1），最终本地手术定位恢复。纪律固化：长文件一律 base64 分段 + sha256 验证后才落盘提交。
2. **口径漂移属历史遗留**：向导 422 暴露 InputView 时代的前后端阈值不一致（0.5 vs gt=0.5），说明"接口契约冻结"后仍需守门测试覆盖边界值。
3. **验收汇总口径修正**：汇总行原在脚本中部导致 W19 段 4 条"跑而不计"，挪至末尾后 43→49 为口径修正而非新增通过项，如实记录。

## 论文素材积累
- 案例库覆盖空档可由谱系图直观呈现（高阻力段稀疏）。
- 数据完备性分析：厂商字段覆盖 32.5%、重量覆盖 8.2%，如实呈现不回避。
- 数据模型局限：合并 source 串无法字段级回溯，33 条"来源待回溯"。

## 欠账滚动（截至 W30 末）
- 案例扩充 41→60（+5/周，盲测 8→15 区）——持续；
- 33 条来源待回溯；潞安煤厚 NULL×2；gas_level_norm 14 条/roof_class 12 条 NULL 回源补录；
- 南梁/麻地沟/通安坐标核实回填；ZY12000/28/62D center_dist=1.50 待厂家核实（已标 suspect）；
- 小债：RecommendReq schema 死代码、entropy_weight.py 单案例 log(1)=0 警告、CompareView 未覆盖 badge（可选）。

## 下周计划（W31）
进入 Stage C 准备：对话选型能力强化与盲测区扩充启动，具体按 W31 计划单执行。
