
# W13 周报：FastAPI 骨架与首个真实接口

## 完成
- FastAPI 最小应用 → 三种传参 → Pydantic 深度校验 → 骨架分层(routers/core/models) → get_db 依赖注入 → /api/areas 真实接口
- 问题8闭环：计算参数从 param_dependencies 读库（含 varchar→float 转换）
- 项目大体检收尾：问题6(备份三线)、问题7(Key外置+历史清洗)全闭环

## 坑与解决
1. .env 中 DB_PASSWORD 与 audit.sh 不一致导致 1045 —— 以 .env 为唯一权威来源
2. param_value 是 varchar，计算参数需 _to_number 转换
3. UPDATE 匹配 0 行不报错 —— 操作前先 SELECT 确认目标行存在
4. nano 粘贴 Python 代码缩进错乱 —— 改用 cat << 'EOF' 写入

## 下周计划
W14：support_models 真实查询接口 + 选型推荐逻辑接 Params + pytest
