# W18 周记：四工具封装与 Agent 组装

## 完成
- D1 工具1 query_database：白名单参数化查询（禁裸SQL）
- D2 工具2 run_matching：CBR 引擎 LLM 友好封装（文字摘要输出）
- D3 工具3 recalc_params：缸径/压力重算（复用 core.support 公式类，η读库；物理验证(360/320)²=1.2656精确吻合；顺带修复 core/db.py 硬编码密码）
- D4 工具4 search_knowledge：MT/T 556 标准轻量 RAG（9块入库，2-gram检索，带来源页码）
- D5 Agent 组装：create_tool_calling_agent + 铁律 prompt（数字必须来自工具/注明来源/禁编造）；解决 langchain 1.0 → langchain-classic 迁移
- D6 四类问题验证：选型/重算(自主双调用)/规范/查库 工具选择全对；顺带修复煤层厚度上限 12→25m
- D7 Agent 入主接口 /api/chat（tools 字段透传调用链），14 测试全绿

## 架构决策（论文素材）
- 自主决策 Agent（function calling）替代固定流程：模型自选工具
- 铁律：一切数字必须来自工具返回 → 架构级防幻觉
- 白名单参数化工具，禁止 LLM 生成裸 SQL
- 流式为"Agent完成后分片推送"，token级真流式需 LangGraph（后续）
