# W17 周记：LLM 接入与流式对话

## 完成
- D1: ChatOpenAI 单例接智谱 GLM（temperature=0，.env 读 Key）
- D2: POST /api/chat 一次性对话（系统提示词限定选型领域）
- D3: SSE 原理实验（text/event-stream、data:\n\n 格式、curl -N）
- D4: 双模流式改造——stream=true 走 StreamingResponse 打字机输出
- D5: 会话管理——chat_messages 表 + session_id + 最近10轮历史进上下文
- D6: 截断验证(10轮上限) + curl -N 全流程验收 + 11 个 pytest 全绿

## 架构决策（论文素材）
- LLM 只做解释不做决策：选型由 CBR 引擎产出，LLM 负责自然语言转写
- temperature=0 保证回答稳定可复现
- 测试全部 mock LLM，不依赖外部服务
- 流式异常降级：[ERROR] 帧 + [DONE] 结尾，不裸 500
