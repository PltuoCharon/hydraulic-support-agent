# W15 周报：CBR 匹配引擎（论文方法章骨架成型）

## 完成项
1. 案例库定型：working_conditions ⋈ mining_areas ⋈ support_models 三表关联，36条案例100%关联实际用架
2. 特征体系：数值型4项(采高/倾角/f/埋深) min-max归一化；分类型3项(顶板/矿压/瓦斯) 打分表(一致1/相邻0.5/不符0/缺失中性0.5)
3. 野生文本归一化映射：roof 8种写法→4级、pressure 8种写法→4级、gas 5种写法→3级（ROOF/PRESSURE/GAS_MAP），
   规则同步沉淀至 dict_enum 表（代码与数据库双轨）
4. AHP定权：判断矩阵→特征向量法→CR=0.0145<0.1通过；权重=[采高0.42, 阻力0.26, 顶板0.15, 矿压0.10, 倾角0.07]
5. POST /api/match：留一法(LOO)排除目标矿区自身案例 + area_id与手动工况互斥校验 + Top-N按架型去重
6. 可解释diffs：每个推荐附"案例采高±差值/案例用架及阻力/不匹配警告"人话说明

## 实测验证（论文实证素材）
- 补连塔12514(8.8m浅埋大采高)：Top-1=同矿22304工作面(sim=0.9035)，推荐ZY21000/38/82D，
  与该矿工程实际选型一致；0.096的相似度差=采高差(6.8vs8.8)×采高权重0.42，权重设计获数值验证
- 互斥校验/404/LOO排除(34=36-2)均按预期工作

## 坑与解决
1. mining_areas.support_model全空 → 案例库改用working_conditions(36条全关联)，CBR走标准路径
2. 分类型字段野生文本(如"平均来压步距16m") → 归一化映射层，并纳入dict_enum双轨管理
3. 相似度1.0=自己匹配自己(数据泄露) → 留一法排除自身案例，盲测必备
4. 请求参数混传被静默忽略 → 互斥校验显式422
5. sed误改文件致IndentationError → 复杂修改一律 cat> 整体重写，不用sed

## 下周计划（W16）
盲测集设计与推荐效果评估 / RAG问答链路联调（按总计划）
