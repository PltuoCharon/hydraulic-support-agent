# W29 周报：归一列接入 + v3 全量重跑 + 地图选区 + 欠账核销

## 本周完成

### D1 归一列接入匹配链路
- CASE_SQL 增查 case_roof_class / case_gas_norm，roof_class_to_level() 转换，NULL→中性 0.5，不猜野生文本
- GAS_MAP/ROOF_MAP 退出匹配链路（"涌出量大"→"高瓦斯"的无依据映射随之失效）
- 寺河改判"数据问题"：实际用架 ZY21000/38/82D 为 suspect，结构性不可命中，非算法缺陷

### D2 v3 全量重跑（冻结纪律：一次行为变化一次重跑，单一归因）
- Top-1 = Top-3 = 3/8 = 37.5%（命中朱仙庄/黄玉川/鲍店），与 v2 持平
- ahp / combo 口径 25% → 37.5%（+12.5pp）
- 敏感性 7 组全部 37.5%，零翻盘，结果稳健
- FREEZE_manifest.txt 追加 v3 行（sha256 + v3_at）
- 样本量警告维持：n=8，95% CI ≈ ±23pp，结论只作趋势不作定论

### D3 统计口径下沉 + 地图数据准备
- stats.overview() 下沉 services/stats.py，areas 计数 WHERE is_test=0（31→23，口径变化已记录）
- mining_areas 增 adcode/lng/lat 列，坐标回填 28/31；南梁/麻地沟/通安 3 处不猜，留 NULL 待核实
- 备份 hs_w29d3_pre_alter.sql（301395B，ALTER 前验证非空）

### D4–D6 地图选区全链路
- GET /api/mining-areas/（is_test=0 AND lng IS NOT NULL → 20 点，契约：盲测区永不出现在前端）
- GET /api/mining-areas/{id}/supports（排除 suspect）
- 前端 /map：GeoJSON 本地化离线渲染，effectScatter 涟漪点，信息卡（地质参数+在用支架），"按此矿区条件选型"按钮 → prefillFromArea → /input 预填 → 一键匹配
- 实测链路：淮北涡北 → Top1 ZF15000/25/45（80%）；断网验收通过
- 页脚口径声明：坐标为地级市近似位置

### D7 可信度角标（核销 W25 前端欠账）
- CASE_SQL 补 support_source / support_status，ResultView badge()：疑(danger)/估(warning)/缺(info)/实(success)，tooltip 显示 source 原文
- 估算标签三处可区分（DB source 字段 / 前端角标 / 论文表述）闭环
- 口径备忘：角标读 source 原文；参数表"未查到公开参数"是前端 NULL 兜底文案，两者不矛盾，论文中需写明

## 欠账核销与遗留

| 欠账 | 状态 |
|---|---|
| W25 前端可信度角标 | ✅ 已核销（D7） |
| 归一列"接而不用" | ✅ 已核销（D1+D2 重跑证明） |
| 前端/目录真相（web/ 而非 frontend/） | ✅ 已核销（D5 纠正） |
| 案例 41<60、盲测区各 1 案 | ⏳ 转 W30+ 持续（+5/周） |
| 33 条来源待回溯 | ⏳ 转 W30+ |
| 潞安煤厚 NULL×2、gas/roof 归一列 NULL 回源 | ⏳ 转 W30+ |
| 南梁/麻地沟/通安坐标 | ⏳ 待核实，不猜 |
| ZY12000/28/62D center_dist=1.50 | ⏳ 待厂家核实（DB 已标 suspect） |

## 工程质量
- 验收 41/41（"无未提交改动"项已覆盖未跟踪文件，zz_probe 验证）
- pytest 全绿（含一致性 11/11、services 守门、地图过滤、suspect 排除）
- 接口契约 W28 冻结后首次按流程变更：改契约（CASE_SQL 加列）→ 守门测试 → 全量回归

## 下周（W30）计划
- 架型谱系图（X=阻力 Y=采高 scatter，空心点=估算）
- 厂商分布图（郑煤机 38 家居首）
- 选型向导四步状态机 + 三入口 Tab 整合
- 首页接真实 stats
