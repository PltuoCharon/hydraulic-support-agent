
## W25-D1 OpenModelica 安装结论（2026-09-10）
- which omc 结果：/usr/bin/omc
- omc --version：初报 liblapack.so.3 加载失败；复验时 liblapack3 已就位，输出 OpenModelica 1.27.0
- 结论：已装通（OpenModelica 1.27.0），W35-D6 作为 scipy 版的对照实现；主线仍为 scipy solve_ivp 自写 ODE
