# W28-D1 重复逻辑对照表（定稿 2026-09-14）

## 结论：真重复 1 处（#2 候选池口径），半重复 1 处（#4 查询SQL），僵尸 1 处（#5），其余干净

| # | 逻辑 | 位置A | 位置B | 结论 | 去向 |
|---|------|-------|-------|------|------|
| 1 | 有效采高 _eff_h | matcher.py | filter.py | 不重复：matcher.py:86-88 已调用 filter.effective_height()，SQL 只取原始列 | 无 |
| 2 | 支架候选池口径 | supports.py:65 过滤 data_status='verified' | matcher.py CASE_SQL 不过滤 | 不一致：匹配会把 suspect 型号(黄玉川 id=16 等)推给用户，列表接口不会 | D2：统一 load_supports()，默认排除 suspect，参数可放行 |
| 3 | CBR 匹配 | matcher.run_match（取数+评分混合） | tools.run_matching（薄壳委托） | 不重复，但 run_match 不可脱离 DB 在 REPL 复用 | D2：拆 load_cases() + 纯函数 match_supports(cond)->list[MatchResult] |
| 4 | 详情/列表查询 | areas.py:14,36 / supports.py:19,37（SQL 在 router） | tools.query_database（自带白名单 SQL） | 半重复：两处"按条件取行"各写各的 | D3：下沉 services/queries.py；D4：query_database 改挂、删自带 SQL |
| 5 | POST /supports/recommend | supports.py:44 | — | 僵尸占位（"D4 接入真实筛选"从未实现） | D3：删除，避免文档与行为不符 |

## 已核查无重复项
- 参数重算：tools.recalc_params → support_calc.recalc（薄壳委托）
- 硬约束筛选：supports.filter_endpoint → filter.filter_supports（取数/纯函数分工正确）
- 熵权 entropy_weight.py / AHP ahp.py：各一份
- 知识检索 knowledge.py：单份
- chat.py 仅展示层读 similarity，无逻辑重复

## 对 D2 的直接约束
1. match_supports 必须是纯函数：输入 ConditionInput + 候选列表，不碰 DB
2. 候选池加载(load_cases/load_supports)是唯一允许写 SQL 的地方，data_status 口径在此统一定义
3. matcher 对 suspect 型号的现有行为改变 = 匹配结果可能变化 → D5 一致性测试前先记录基线，变化要在提交信息中说明
